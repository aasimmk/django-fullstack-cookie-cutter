# Frontend tests (Next.js)

- **Unit:** `npm run test` / `pnpm run test` / `yarn test` — Vitest (`tests/unit/`).
- **E2E:** `npm run test:e2e` — Playwright; starts the Next dev server on port **3000** automatically.

Run Django on **8000** separately when exercising API flows from the browser.
