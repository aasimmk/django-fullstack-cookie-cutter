-- Runs once on first database init (empty data directory).
-- Add CREATE EXTENSION and other DDL here, or add more .sql / .sh files in this directory.
-- See docker/postgres/README.md for installing OS-level Postgres plugins in the Dockerfile.

SELECT 1;
